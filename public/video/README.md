Optional: drop a real opening video here as:

    hall-of-fame-opening.mp4

If present, it autoplays (muted) as the cinematic opening instead of
the portrait Ken Burns sequence. If it's missing or fails to load,
the site falls back to the image sequence automatically — no code
changes needed either way.
